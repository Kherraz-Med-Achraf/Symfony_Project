{% extends 'base.html.twig' %}

{% block title %}Liste des Articles{% endblock %}

{% block body %}



    <h1>Liste des Articles  {% if brouillon %} Brouillons {% endif %} ! </h1>


    <ul>
    {% for article in articles %}
        <li><a href="{{ path('vue_article', { 'id': article.id }) }}">{{ article.titre }}</a></li>
    {% endfor %}
    </ul>

{% endblock %}
